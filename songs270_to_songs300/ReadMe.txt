1. Upload the folder "upgrade" to the new version main folder,
2. add the following values to the config.php which pointing to the old version database info,


$old_db_host = "localhost";
$old_db_name = "username_songs270";
$old_db_username = "username";
$old_db_password = "password";


3. open the url "/upgrade" in the browser 